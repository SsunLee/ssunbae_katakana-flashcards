// app/next.config.js

/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',

  // (기존에 다른 설정이 있었다면 그대로 둡니다)
};

// module.exports = nextConfig; 대신 아래 코드를 사용합니다.
export default nextConfig;

